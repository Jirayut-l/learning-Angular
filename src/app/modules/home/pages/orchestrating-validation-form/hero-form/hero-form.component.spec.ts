import { HeroFormComponent } from './hero-form.component';

describe('HeroFormComponent', () => {
  let component: HeroFormComponent;
  beforeEach(() => {
    component = new HeroFormComponent();
  });
  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
